RewriteEngine On

# Redirect 301 from /menu to /
RewriteRule ^menu/?$ / [R=301,L]

# Handle SPA routing - serve index.html for all routes
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule . /index.html [L]
